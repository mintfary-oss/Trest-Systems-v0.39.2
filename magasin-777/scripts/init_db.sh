#!/bin/bash
set -e

# ============================================
# LocalMarket - Legacy DB Init Script
# ============================================
# NOTE: Database initialization is now handled automatically
# by the backend service (SQLAlchemy models + seed data).
# This script is kept for reference only.
#
# Tables, admin user, demo data, and 300 themes are all
# created on first startup of the evaluator-service.

echo "=== Database initialization is handled by the backend service ==="
echo "=== This script is no longer needed ==="
