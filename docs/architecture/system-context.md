# System Context

Пользователь управляет self-hosted Trest Systems через `trestctl`. CLI управляет Docker Compose, локальной инфраструктурой и состоянием установки. Бизнес-сервисы подключаются поэтапно.
